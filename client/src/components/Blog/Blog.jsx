import React from 'react'
import { useState, useEffect } from 'react'
import { createPost, getPost, updatePost, deletePost } from '../../api/postApi';

export const Blog = () => {
    const blogs = useState([]);
    
    useEffect (() => {
        
    })
  return (
    <div>Blog</div>
  )
}
